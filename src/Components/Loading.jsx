import React from "react";
import ReactLoading from "react-loading";

function Loading() {
  return <ReactLoading type={"bubbles"} color={"#ffffff"} height={100} width={200} />;
}

export default Loading;
